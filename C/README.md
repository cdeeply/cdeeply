C/C++ library to call C Deeply's supervised and unsupervised neural network generators.  Requires [libcurl](https://curl.se/libcurl/).

To compile the example file, enter:

gcc cdeeply.c example.c -o example -lm -lcurl
